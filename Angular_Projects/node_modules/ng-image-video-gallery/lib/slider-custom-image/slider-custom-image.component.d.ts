import { OnChanges, SimpleChanges, EventEmitter } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { NgImageSliderService } from './../ng-image-slider.service';
import { SliderService } from '../services/slider.service';
export declare class SliderCustomImageComponent implements OnChanges {
    imageSliderService: NgImageSliderService;
    private sanitizer;
    private sliderService;
    YOUTUBE: string;
    IMAGE: string;
    VIDEO: string;
    ANOTHER_VIDEO: string;
    fileUrl: SafeResourceUrl;
    videoUrl: SafeResourceUrl;
    fileExtension: string;
    type: string;
    imageLoading: boolean;
    wistiaUrl: string;
    showVideo: boolean;
    videoAutoPlay: boolean;
    showVideoControls: number;
    currentImageIndex: number;
    imageIndex: number;
    speed: number;
    imageUrl: any;
    isVideo: boolean;
    alt: String;
    title: String;
    direction: string;
    ratio: boolean;
    lazy: boolean;
    imgData: any;
    imgHeight: any;
    getThumbnail: EventEmitter<any>;
    imagePositionInSlider: any;
    constructor(imageSliderService: NgImageSliderService, sanitizer: DomSanitizer, document: any, sliderService: SliderService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    setUrl(): void;
    videoClickHandler(event: any): void;
}
